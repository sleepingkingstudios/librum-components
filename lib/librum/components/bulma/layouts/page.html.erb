<%= render(build_header) %>

<main class="section pt-5" style="flex: 1;">
  <div class="<%= container_class_name %>">
    <%= render_alerts %>

    <%= content %>
  </div>
</main>

<%= render(build_footer) %>
